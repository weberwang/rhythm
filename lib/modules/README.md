# modules

该目录是目标蓝图中的正式有界模块承载位。

当前仓库仍以 `lib/features/*` 为主，因为现有功能和测试已经较多，本次初始化补齐不做高风险整仓迁移。

使用约定：

- 新的大型结构治理任务可以把新模块或迁移后的模块落到这里。
- 普通业务任务不要顺手把整个 `features/*` 大迁移到这里。
- 在迁移窗口到来前，继续按有界上下文思维维护 `features/*` 的边界即可。
