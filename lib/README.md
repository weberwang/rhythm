# Rhythm Lib Scaffold

当前 `lib/` 目录只完成初始化骨架，不包含真实启动代码、路由实现或业务页面实现。

- `app/`：后续 bootstrap、router、theme、entry 的宿主目录
- `core/`：跨 feature 的基础能力边界
- `shared/`：共享设计与通用 widgets
- `features/`：按 DDD-by-feature 划分的业务模块

真实运行时代码留给后续 `bootstrap code` 与 feature implementation 阶段。
