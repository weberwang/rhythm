interface:
  display_name: "GPT Image 2 Generator"
  short_description: "Generate local images through custom gpt-image-2 API"
  default_prompt: "Use $gpt-image-2-generator to generate one or more local images with gpt-image-2 through a custom base URL and API key loaded from environment variables."
