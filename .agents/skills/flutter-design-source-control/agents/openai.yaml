interface:
  display_name: "Flutter Design Source Control"
  short_description: "Control frozen design sources and post-freeze changes"
  default_prompt: "Use $flutter-design-source-control to enforce the frozen UI/UX RD, theme artifacts, visual evidence, and module design-source packet after freeze, route post-freeze UI changes back through design approval, and prevent code-stage redesign."
