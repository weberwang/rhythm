interface:
  display_name: "Flutter Design Freeze Gate"
  short_description: "Enforce explicit design approval before Pen or Flutter work"
  default_prompt: "Use $flutter-design-freeze-gate to verify that a Flutter shared or module design freeze has an approved UI/UX packet, a completed fresh-subagent visual-design-reviewer result on the freeze-facing visual draft itself, a score of at least 90 with no unresolved information-hierarchy or key-task-guidance failures, immutable design constraints, and the right approval state before module splitting, Pencil, or code work proceeds."
