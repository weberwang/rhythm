# app_foundation 工作流记录

> 模块：`app_foundation`
> 日期：2026-06-02
> 当前状态：`pen_ready`

## 1. 当前检查结果

- UI/UX RD 已升级为商业级设计草案并获得显式批准。
- 实现 RD 已明确基础层职责和收口方向。
- `pen/v3.pen` 已被指定为唯一设计源。
- 已写入基础变量、共享组件和五类基础壳层演示对象。
- `snapshot_layout(problemsOnly=true)` 结果为无结构问题。
- 已对组件区和一级壳完成截图核对。

## 2. 设计冻结门判断

`freeze_decision`：`frozen_for_pen`

原因：

- 商业级 UI/UX 方向已存在
- 用户已明确确认继续执行
- `v3.pen` 中已经存在可核对的基础层设计对象

## 3. 阻塞项

- 当前仍缺少用户对首轮 Pencil 结果的复核确认
- 还不能把模块标记为 `pen_frozen`
- 还不能进入基于冻结设计稿的 Flutter 落地

## 4. 允许的下一步

- 基于当前 Pencil 草稿做一轮用户评审
- 根据评审意见微调基础组件或壳层
- 评审通过后把模块标记为 `pen_frozen`

## 5. 禁止的下一步

- 不能跳过评审直接把当前 Pencil 草稿当作最终冻结设计
- 不能把旧的 `pen/app.pen` 或 `pen/new-app.pen` 当作本模块替代设计源
- 不能让业务模块各自定义基础按钮、卡片、页壳和反馈风格
